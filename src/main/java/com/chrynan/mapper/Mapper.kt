package com.chrynan.mapper

/**
 * An interface that represents a class that can map between objects. This is mainly used for scoping but can have common
 * functions available to all [Mapper] implementations.
 */
interface Mapper