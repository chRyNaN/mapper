package com.chrynan.mapper

/**
 * An interface that represents a class that can format an object. This is mainly used for scoping but can have common
 * functions available to all [Formatter] implementations.
 */
interface Formatter